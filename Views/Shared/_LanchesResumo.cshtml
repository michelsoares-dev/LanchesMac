@model Lanche

<div class="col-sm-4">
    <img src="@Model.ImagemUrl" alt="" />
    <h3 class=text-left> @Model.Preco.ToString("c") </h3>
    <p>@Model.Nome</p>
    <p>@Model.DescricaoCurta</p>
</div>
